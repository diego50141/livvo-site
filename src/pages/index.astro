---
import Base from '../layouts/Base.astro';
import Navbar from '../components/Navbar.astro';
import Button from '../components/Button.astro';
import Section from '../components/Section.astro';
import PricingCard from '../components/PricingCard.astro';
import FAQ from '../components/FAQ.astro';
import Conversation from '../components/Conversation.astro';
import ProductStack from '../components/ProductStack.astro';
import DealFlow from '../components/DealFlow.astro';
import Footer from '../components/Footer.astro';
import { WHATSAPP_URL } from '../config';

// Sección de precios oculta por ahora; volver a true para mostrarla
// (también restaura el link "Precios" en Navbar.astro y el alt del FAQ).
const SHOW_PRICING = false;

// Conversación del hero: se reproduce en loop, turno a turno, y cuenta el
// ciclo completo — responde, califica, recomienda, agenda y entrega el lead.
const heroChat = [
  { from: 'client' as const, label: 'Carolina', text: 'Hola, vi el apartamento en Chapinero. ¿Sigue disponible?' },
  { from: 'luisa' as const, text: '¡Hola, Carolina! Sí, sigue disponible. ¿Lo buscas para compra o arriendo?' },
  { from: 'client' as const, label: 'Carolina', text: 'Para compra, con un presupuesto de $450 millones.' },
  { from: 'reasoning' as const, label: 'Luisa calificando' },
  {
    from: 'luisa' as const,
    text: 'Perfecto. Tengo 3 opciones en Chapinero y Chicó dentro de tu presupuesto. ¿Te gustaría verlas este sábado?',
  },
  { from: 'client' as const, label: 'Carolina', text: 'Sí, en la mañana.' },
  {
    from: 'luisa' as const,
    text: 'Listo, agendé tu visita el sábado a las 10:00 a.m. Un asesor te confirma hoy mismo.',
  },
];

// Pantallas fijas que rotan con el scroll (patrón useplaza): la sección mide
// varias alturas de viewport y el contenido queda "pegado" mientras los
// pasos 01 → 04 se cruzan con fade + desplazamiento.
const steps = [
  {
    n: '01',
    title: 'Unifica tu operación comercial y de servicio al cliente',
    desc: 'Renta, venta, compra, mantenimiento y jurídico en un solo lugar: una línea de WhatsApp, tu equipo completo y empleados IA con un flujo definido para cada caso.',
    ctaLabel: 'Conversar con Livvo →',
    ctaHref: WHATSAPP_URL,
    ctaVariant: 'primary',
  },
  {
    n: '02',
    title: 'Centraliza tu portafolio de propiedades y tus clientes',
    desc: 'Tus empleados de IA se conectan a la base de propiedades, procesos y clientes de tu inmobiliaria, y mantienen la información al día.',
    ctaLabel: 'Conversar con Livvo →',
    ctaHref: WHATSAPP_URL,
    ctaVariant: 'primary',
  },
  {
    n: '03',
    title: 'Crea tus propios empleados IA',
    desc: 'Defines el flujo de cada caso y sales a producción sin escribir una línea de código ni depender de tu área de tecnología.',
    ctaLabel: 'Conversar con Livvo →',
    ctaHref: WHATSAPP_URL,
    ctaVariant: 'primary',
  },
  {
    n: '04',
    title: 'Mide y mejora',
    desc: 'Tiempos de respuesta, conversión por canal y productividad por asesor, en tiempo real.',
    ctaLabel: 'Conversar con Livvo →',
    ctaHref: WHATSAPP_URL,
    ctaVariant: 'primary',
  },
] as const;

// Sección Producto: dos piezas que se explican mutuamente — la Plataforma
// Comercial donde trabaja el equipo humano y los Empleados IA que atienden.
const products = [
  {
    key: 'admin',
    name: 'Plataforma Comercial',
    href: '/plataforma-comercial',
    tagline:
      'Donde tu equipo convierte: bandeja compartida, calendario, integraciones y métricas de toda la operación, con los leads que tus Empleados IA entregan listos.',
    features: [
      {
        icon: 'chat',
        title: 'Bandeja de equipo para WhatsApp',
        desc: 'Todo tu equipo atiende desde un solo lugar, con asignación de conversaciones y visibilidad de cada chat.',
      },
      {
        icon: 'calendar-check',
        title: 'Calendario de citas del equipo',
        desc: 'Todas las visitas que agendan tus Empleados IA quedan organizadas en el calendario de tus asesores.',
      },
      {
        icon: 'link',
        title: 'Integración con CRM y ERP',
        desc: 'Conectada con los principales sistemas del mercado inmobiliario. Todo en un solo lugar.',
      },
      {
        icon: 'chart',
        title: 'Informes y métricas',
        desc: 'Rendimiento en tiempo real: conversaciones, atención al cliente, tasas de conversión y productividad.',
      },
    ],
  },
  {
    key: 'luisa',
    name: 'Empleados IA',
    href: '/empleados-ia',
    tagline:
      'Empleados de Inteligencia Artificial en cada punto de contacto con tus clientes.',
    features: [
      {
        icon: 'chat',
        title: 'Atención al cliente 24/7',
        desc: 'Luisa responde en segundos, con el tono de tu inmobiliaria, a inquilinos y propietarios en cualquier momento.',
      },
      {
        icon: 'filter',
        title: 'Calificación automática de leads',
        desc: 'Detecta quién realmente desea alquilar o comprar, captura presupuesto, zona y liquidez, y te entrega solo los leads listos para avanzar.',
      },
      {
        icon: 'home',
        title: 'Recomendación de inmuebles',
        desc: 'Cada lead recibe propiedades de tu portafolio que encajan con lo que busca, directo en el chat.',
      },
      {
        icon: 'calendar',
        title: 'Agendamiento de visitas',
        desc: 'Luisa coordina la cita en la misma conversación, sin idas y vueltas de mensajes con tu equipo.',
      },
    ],
  },
];

const featureIcons: Record<string, string> = {
  chat: '<path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"/>',
  filter: '<path d="M22 3H2l8 9.46V19l4 2v-8.54L22 3z"/>',
  target: '<circle cx="12" cy="12" r="10"/><circle cx="12" cy="12" r="6"/><circle cx="12" cy="12" r="2"/>',
  home: '<path d="M3 9.5 12 3l9 6.5"/><path d="M5 8.5V21h14V8.5"/><path d="M9 21v-6h6v6"/>',
  calendar:
    '<rect x="3" y="4" width="18" height="18" rx="2"/><path d="M16 2v4M8 2v4M3 10h18"/><path d="m9 16 2 2 4-4"/>',
  'calendar-check':
    '<rect x="3" y="4" width="18" height="18" rx="2"/><path d="M16 2v4M8 2v4M3 10h18"/><path d="M8 15h3M8 18h6"/>',  link: '<path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/>',
  chart: '<path d="M3 3v18h18"/><path d="M7 15v3M12 10v8M17 6v12"/>',
};

const plans = [
  {
    name: '🚀 Plan Esencial',
    price: '$499.000',
    period: 'COP / mes',
    tagline: 'Para negocios que están empezando con IA',
    features: [
      'Hasta 500 conversaciones mensuales',
      'Operación 24/7 de tus Empleados IA',
      'Hasta 5 usuarios',
      'Ajustes y mejoras continuas',
      'Soporte básico',
    ],
    featured: false,
    ctaLabel: 'Comenzar ahora',
  },
  {
    name: 'Plan Profesional',
    price: '$749.000',
    period: 'COP / mes',
    tagline: 'Para negocios con alto volumen de mensajes',
    features: [
      'Hasta 1.000 conversaciones mensuales',
      'Operación 24/7 de tus Empleados IA',
      'Usuarios ilimitados',
      'Campañas de WhatsApp',
      'Módulo de plantillas de Meta',
      'Reunión mensual de optimización',
      'Ajustes y mejoras continuas',
      'Soporte prioritario',
      'Gerente de cuenta dedicado',
    ],
    featured: true,
    ctaLabel: 'Comenzar ahora',
  },
  {
    name: 'Plan Corporativo',
    price: 'A medida',
    period: '',
    tagline: 'Para operaciones que necesitan más',
    features: [
      'Volumen de conversaciones a medida',
      'Operación 24/7 de tus Empleados IA',
      'Usuarios ilimitados',
      'Campañas de WhatsApp',
      'Módulo de plantillas de Meta',
      'Reunión mensual de optimización',
      'Ajustes y mejoras continuas',
      'Soporte premium 24/7',
      'Gerente de cuenta dedicado',
      'Canales adicionales (Instagram, Messenger, etc.)',
      'Acceso al API de Livvo',
    ],
    featured: false,
    ctaLabel: 'Conversar con Livvo →',
  },
];

// Integraciones — logos oficiales en public/integrations/.
// Dos cintas: CRMs (arriba) y portales inmobiliarios (abajo).
const integrationRows = [
  [
    { name: 'HubSpot', cat: 'CRM', src: '/integrations/hubspot.svg' },
    { name: 'GoHighLevel', cat: 'CRM', src: '/integrations/gohighlevel.svg' },
    { name: 'Wasi', cat: 'CRM inmobiliario', src: '/integrations/wasi.svg' },
    { name: 'Domus', cat: 'CRM inmobiliario', src: '/integrations/domus.png' },
  ],
  [
    { name: 'Fincaraíz', cat: 'Portal', src: '/integrations/fincaraiz.png' },
    { name: 'Metrocuadrado', cat: 'Portal', src: '/integrations/metrocuadrado.png' },
    { name: 'Ciencuadras', cat: 'Portal', src: '/integrations/ciencuadras.svg' },
    { name: 'Century 21', cat: 'Red inmobiliaria', src: '/integrations/century21.svg' },
  ],
];

const faqItems = [
  {
    q: '¿Qué hace exactamente Livvo?',
    a: 'Livvo es el sistema comercial de IA para inmobiliarias que integra en una sola plataforma conversaciones, negocios y agentes de inteligencia artificial. El objetivo es eliminar el trabajo operativo de tu equipo y aumentar tu tasa de cierre.',
  },
  {
    q: '¿La IA reemplaza a mis asesores?',
    a: 'No. Imagínalos como una primera línea que nunca duerme: los Empleados IA responden, califican y agendan; tus asesores entran cuando hay una visita que atender o un negocio que cerrar, con todo el historial de la conversación a la vista, y pueden intervenir en cualquier momento si lo ven necesario.',
  },
  {
    q: '¿Qué pasa si la IA no sabe responder algo?',
    a: 'Escala la conversación a un asesor humano en el mismo chat, sin que el cliente tenga que repetir nada. Además tú defines el nivel de autonomía: puede responder sola o dejar el borrador listo para que alguien lo apruebe antes de enviarlo.',
  },
  {
    q: '¿Sirve solo para vender, o también para arriendos y postventa?',
    a: 'También. Los mismos Empleados IA atienden a inquilinos y propietarios: requisitos, mantenimiento, renovaciones y estado de un proceso, sin que tu equipo tenga que responder caso por caso.',
  },
  {
    q: '¿Funciona con los leads que llegan de los portales?',
    a: 'Sí. Los leads de Fincaraíz, Metrocuadrado, Ciencuadras o tu propia página entran a la misma bandeja y reciben respuesta en segundos, sin importar la hora.',
  },
  {
    q: '¿Puedo seguir usando el número de WhatsApp que ya tiene mi inmobiliaria?',
    a: 'Sí. Migramos tu línea actual a WhatsApp Business API para que tus clientes sigan escribiendo al mismo número, y todo el equipo atiende desde una bandeja compartida en vez de desde celulares sueltos.',
  },
  {
    q: '¿De quién son los contactos y las conversaciones?',
    a: 'De tu inmobiliaria. Contactos, historial y negocios viven en la plataforma, no en el celular del asesor: si alguien se va del equipo, la relación con el cliente se queda.',
  },
  {
    q: '¿Cuánto tiempo toma implementarlo?',
    a: 'Entre 1 y 3 días dependiendo de la personalización. Incluye entrenamiento del agente y conexión con WhatsApp Business API.',
  },
  {
    q: '¿De qué depende el precio?',
    a: 'Del volumen de conversaciones al mes, del número de propiedades que administres y de cuántos usuarios de tu equipo entran a la plataforma.',
  },
  {
    q: '¿Qué pasa si quiero cancelar?',
    a: 'No hay contrato de permanencia — cancelas cuando quieras, sin penalidades. Te acompañamos durante la implementación para asegurarnos de que veas resultados desde el primer mes.',
  },
];

const faqSubtitle = 'Más información sobre Livvo';
---

<Base>
  <Navbar />

  <main>
  <!-- HERO — partículas teal como energía ambiental -->
  <div class="hero-scene">
    <div id="hero-particles" class="hero-scene__particles" aria-hidden="true"></div>
    <header class="hero">
      <div class="container-site hero__inner">
        <div class="hero__content" id="hero-content">
          <h1 class="hero__title">
            El sistema comercial de <em class="grad-text">IA para inmobiliarias.</em>
          </h1>
          <p class="hero__sub">
            Livvo opera todo el ciclo comercial y de atención al cliente de tu inmobiliaria
            —de la prospección a la postventa— con agentes de IA que trabajan 24/7 y que puedes
            gestionar en una sola plataforma. Se integra con tus sistemas actuales.
          </p>
          <div class="hero__ctas">
            <Button href={WHATSAPP_URL} target="_blank" size="lg" id="hero-demo-cta">Conversar con Livvo →</Button>
          </div>
        </div>
        <div class="hero__chat">
          <DealFlow />
        </div>
      </div>
    </header>
  </div>

  <!-- CÓMO FUNCIONA — pantallas fijas que pasan con el scroll (estilo useplaza) -->
  <section id="como-funciona" class="steps" aria-label="Cómo funciona la plataforma">
    <div class="steps__sticky">
      <div class="container-site steps__inner">
        <div class="steps__nav" aria-hidden="true">
          {steps.map(({ n }, i) => (
            <span class:list={['steps__nav-item', { 'is-active': i === 0 }]} data-step-dot>
              {n}
            </span>
          ))}
        </div>
        <div class="steps__panels">
          {steps.map(({ n, title, desc, ctaLabel, ctaHref, ctaVariant }, i) => (
            <article class:list={['step-panel', { 'is-active': i === 0 }]} data-step-panel>
              <span class="step-panel__num">{n}</span>
              <h2 class="step-panel__title">{title}</h2>
              <p class="step-panel__desc">{desc}</p>
              <div>
                <Button
                  href={ctaHref}
                  variant={ctaVariant}
                  size="lg"
                  target={ctaHref.startsWith('http') ? '_blank' : undefined}
                >
                  {ctaLabel}
                </Button>
              </div>
            </article>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- PRODUCTO -->
  <Section
    id="producto"
    eyebrow="Producto"
  >
    {
      products.map(({ key, name, href, tagline, features }) => (
        <div class="product-block">
          <header class="product-block__head fade-up">
            <a class:list={['product-badge', `product-badge--${key}`]} {href}>{name}</a>
            <p class="product-block__tagline">{tagline}</p>
            <a class:list={['product-block__link', `product-block__link--${key}`]} {href}>
              Conocer {name} →
            </a>
          </header>
          <div class="features-grid fade-up">
            {features.map(({ icon, title, desc }) => (
              <article class:list={['feature-card', `feature-card--${key}`]}>
                <span class="feature-card__icon" aria-hidden="true">
                  <svg
                    viewBox="0 0 24 24"
                    width="22"
                    height="22"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="1.8"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    set:html={featureIcons[icon]}
                  />
                </span>
                <h3 class="feature-card__title">{title}</h3>
                <p class="feature-card__desc">{desc}</p>
              </article>
            ))}
          </div>
        </div>
      ))
    }
  </Section>

  <!-- INTEGRACIONES -->
  <Section
    id="integraciones"
    eyebrow="Integraciones"
    title="Livvo se conecta con los sistemas que ya usas."
    subtitle="Integración con los principales CRMs, portales y ERPs del mercado inmobiliario."
    alt
  >
    <div class="integrations fade-up">
      {integrationRows.map((row, i) => (
        <div class="integrations__marquee">
          <div class:list={['integrations__track', { 'integrations__track--reverse': i % 2 === 1 }]}>
            {[0, 1].map((copy) => (
              <div class="integrations__group" aria-hidden={copy === 1 ? 'true' : undefined}>
                {row.map(({ name, cat, src }) => (
                  <span class="integration-pill" title={`${name} · ${cat}`}>
                    <img class="integration-pill__logo" src={src} alt={name} loading="lazy" />
                  </span>
                ))}
              </div>
            ))}
          </div>
        </div>
      ))}
      <p class="integrations__note">
        ¿Usas otro sistema? <a href={WHATSAPP_URL} target="_blank" rel="noopener">Cuéntanos</a>.
      </p>
    </div>
  </Section>

  <!-- PRECIOS (oculto por ahora — ver SHOW_PRICING) -->
  {SHOW_PRICING && (
  <Section id="precios" eyebrow="Planes" title="Empieza gratis. Escala cuando quieras.">
    <div class="pricing-grid fade-up">
      {
        plans.map((plan) => (
          <PricingCard
            name={plan.name}
            price={plan.price}
            period={plan.period}
            tagline={plan.tagline}
            features={plan.features}
            featured={plan.featured}
            ctaLabel={plan.ctaLabel}
            ctaHref={WHATSAPP_URL}
            ctaTarget="_blank"
          />
        ))
      }
    </div>
    <div class="setup-card fade-up">
      <div class="setup-card__info">
        <div class="setup-card__badge">Pago único · Mes 1</div>
        <div class="setup-card__name">Implementación inicial</div>
        <p class="setup-card__desc">
          Antes de activar tu plan configuramos y entrenamos tu Empleado IA y plataforma para que
          esté listo para trabajar.
        </p>
        <ul class="setup-card__features">
          <li><span class="setup-card__check">✓</span>Sesión de descubrimiento para entender tu negocio</li>
          <li><span class="setup-card__check">✓</span>Configuración y entrenamiento del Empleado IA</li>
          <li><span class="setup-card__check">✓</span>Integración con tu WhatsApp</li>
          <li><span class="setup-card__check">✓</span>Incluye 500 conversaciones el primer mes</li>
        </ul>
      </div>
      <div class="setup-card__price-box">
        <span class="setup-card__from">desde</span>
        <div class="setup-card__price">$1.199.000</div>
        <span class="setup-card__period">COP · pago único</span>
      </div>
    </div>
    <div class="pricing-terms fade-up">
      <span>Prueba gratuita de 7 días</span>
      <span>Sin tarjeta de crédito</span>
      <span>Facturación en pesos colombianos</span>
    </div>
  </Section>
  )}

  <!-- FAQ (alt sigue la alternancia de fondos: sin precios va tras Integraciones, que ya es alt) -->
  <Section id="faq" eyebrow="FAQ" title="Preguntas frecuentes" subtitle={faqSubtitle} alt={SHOW_PRICING}>
    <div class="fade-up">
      <FAQ items={faqItems} />
      <div class="faq-footer fade-up">
        <p>Contáctanos si tienes alguna pregunta o problema.</p>
        <Button href={WHATSAPP_URL} target="_blank" variant="primary">Conversar con Livvo →</Button>
      </div>
    </div>
  </Section>
  </main>

  <Footer />
</Base>

<script>
  // Partículas teal del DS: pocas, pequeñas, deriva lenta y parpadeo sutil.
  // Sin top-level await: si algo falla o se cuelga, no bloquea el módulo.
  import { tsParticles } from '@tsparticles/engine';
  import { loadSlim } from '@tsparticles/slim';

  const initParticles = async () => {
    await loadSlim(tsParticles);
    await tsParticles.load({
      id: 'hero-particles',
      options: {
        fullScreen: { enable: false },
        background: { color: 'transparent' },
        fpsLimit: 60,
        detectRetina: true,
        particles: {
          number: { value: 18, density: { enable: false } },
          color: { value: '#00C7B4' },
          shape: { type: 'circle' },
          opacity: {
            value: { min: 0.3, max: 0.8 },
            animation: { enable: true, speed: 0.5, sync: false },
          },
          size: { value: { min: 4, max: 7 } },
          shadow: { enable: true, color: '#00C7B4', blur: 18, offset: { x: 0, y: 0 } },
          move: {
            enable: true,
            speed: 0.35,
            direction: 'none',
            random: true,
            straight: false,
            outModes: { default: 'out' },
          },
        },
        interactivity: {
          events: { onHover: { enable: false }, onClick: { enable: false }, resize: { enable: true } },
        },
      },
    });
  };

  if (!matchMedia('(prefers-reduced-motion: reduce)').matches) {
    initParticles().catch((e) => console.error('[particles] error:', e));
  }
</script>

<script>
  // Sección "cómo funciona": el contenedor mide 300vh y el contenido queda
  // sticky; el progreso de scroll decide qué paso está activo y los paneles
  // se cruzan con fade + desplazamiento (transiciones CSS → fluido).
  const stepsSection = document.getElementById('como-funciona');
  const stepPanels = Array.from(document.querySelectorAll('[data-step-panel]'));
  const stepDots = Array.from(document.querySelectorAll('[data-step-dot]'));
  const stepsReduced = matchMedia('(prefers-reduced-motion: reduce)').matches;

  if (stepsSection && stepPanels.length && stepsReduced) {
    // sin animación: pasos apilados, todos visibles
    stepsSection.classList.add('steps--static');
  } else if (stepsSection && stepPanels.length) {
    let stepsTicking = false;

    const updateSteps = () => {
      stepsTicking = false;
      const rect = stepsSection.getBoundingClientRect();
      const scrollable = rect.height - innerHeight;
      if (scrollable <= 0) return;
      const progress = Math.min(1, Math.max(0, -rect.top / scrollable));
      // reparte el progreso en tramos iguales por paso
      const idx = Math.min(stepPanels.length - 1, Math.floor(progress * stepPanels.length));

      stepPanels.forEach((panel, i) => {
        panel.classList.toggle('is-active', i === idx);
        panel.classList.toggle('is-past', i < idx);
      });
      stepDots.forEach((dot, i) => dot.classList.toggle('is-active', i === idx));
    };

    addEventListener(
      'scroll',
      () => {
        if (!stepsTicking) {
          stepsTicking = true;
          requestAnimationFrame(updateSteps);
        }
      },
      { passive: true }
    );
    updateSteps();
  }
</script>

<style>
  /* La escena contiene hero + chat y aporta el fondo; los anillos
     pueden desbordar entre secciones sin salirse de la escena */
  .hero-scene {
    position: relative;
    overflow: hidden;
    background: var(--white);
    transition: background-color var(--transition);
  }

  /* Partículas teal (patrón del DS: dispersas, energía ambiental) */
  .hero-scene__particles {
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
  }

  .hero {
    min-height: calc(82vh - 68px);
    display: flex;
    align-items: center;
    background: transparent;
    position: relative;
    z-index: 2;
    padding: 10vh 0;
  }
  /* Hero a dos columnas: mensaje a la izquierda, conversación viva a la derecha */
  .hero__inner {
    width: 100%;
    display: grid;
    grid-template-columns: 1fr minmax(420px, 560px);
    align-items: center;
    gap: var(--space-12);
  }
  .hero__content {
    animation: hero-in 0.7s ease both;
    will-change: transform, opacity;
  }
  @keyframes hero-in {
    from { opacity: 0; transform: translateY(24px); }
    to { opacity: 1; transform: none; }
  }
  .hero__title {
    font-size: clamp(36px, 4vw, 56px);
    letter-spacing: -1.5px;
    line-height: 1.1;
    margin: 0 0 var(--space-6);
  }
  .hero__title em { font-style: normal; }
  .hero__sub {
    font-size: var(--text-body);
    line-height: var(--lh-relaxed);
    color: var(--muted);
    max-width: 520px;
    margin: 0 0 var(--space-8);
  }
  .hero__ctas { display: flex; gap: var(--space-4); flex-wrap: wrap; }
  .hero__chat {
    display: block;
    animation: hero-in 0.7s 0.15s ease both;
  }

  @media (max-width: 900px) {
    .hero__inner {
      grid-template-columns: 1fr;
      gap: var(--space-8);
      justify-items: center;
      text-align: center;
    }
    .hero__sub { margin-inline: auto; }
    .hero__ctas { justify-content: center; }
    .hero__chat { justify-content: center; width: 100%; }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero__content { animation: none; }
  }

  /* ---- Cómo funciona: pantallas sticky que pasan con el scroll ---- */
  .steps {
    position: relative;
    height: 400vh; /* 100vh por paso: define cuánto scroll dura la escena */
    background: var(--gray-bg);
    transition: background-color var(--transition);
  }
  .steps__sticky {
    position: sticky;
    top: 0;
    height: 100vh;
    display: flex;
    align-items: center;
    overflow: hidden;
  }
  .steps__inner {
    display: flex;
    align-items: center;
    gap: var(--space-12);
    width: 100%;
  }
  .steps__nav {
    display: flex;
    flex-direction: column;
    gap: var(--space-8);
    font-family: var(--font-mono);
    font-size: 15px;
    color: var(--muted);
  }
  .steps__nav-item {
    opacity: 0.45;
    transition: opacity 0.4s ease, color 0.4s ease, transform 0.4s ease;
  }
  .steps__nav-item.is-active {
    opacity: 1;
    color: var(--teal-deep);
    font-weight: 700;
    transform: scale(1.25);
  }
  .steps__panels {
    position: relative;
    flex: 1;
    height: min(520px, 72vh);
  }
  .step-panel {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    opacity: 0;
    transform: translateY(70px);
    transition: opacity 0.55s ease, transform 0.55s ease;
    pointer-events: none;
  }
  .step-panel.is-active {
    opacity: 1;
    transform: none;
    pointer-events: auto;
  }
  .step-panel.is-past {
    transform: translateY(-70px);
  }
  .step-panel__num {
    font-family: var(--font-mono);
    font-size: 20px;
    font-weight: 700;
    color: var(--teal-deep);
    margin-bottom: var(--space-4);
  }
  /* mismo tamaño de título y subtítulo que el resto de secciones */
  .step-panel__title {
    font-size: clamp(32px, 4vw, 44px);
    letter-spacing: -1px;
    margin: 0 0 var(--space-6);
    max-width: 720px;
  }
  .step-panel__desc {
    font-size: var(--text-h2);
    line-height: var(--lh-body);
    color: var(--muted);
    max-width: 680px;
    margin: 0 0 var(--space-8);
  }

  /* Fallback sin animación (prefers-reduced-motion): pasos apilados */
  .steps--static { height: auto; }
  .steps--static .steps__sticky { position: static; height: auto; padding: 96px 0; }
  .steps--static .steps__nav { display: none; }
  .steps--static .steps__panels { height: auto; }
  .steps--static .step-panel {
    position: static;
    opacity: 1;
    transform: none;
    pointer-events: auto;
    padding: var(--space-12) 0;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-6);
  }
  .feature-card {
    background: var(--white);
    border: 1px solid var(--border);
    border-radius: var(--radius-card);
    box-shadow: var(--shadow-sm);
    padding: var(--space-6);
    transition: box-shadow var(--transition), border-color var(--transition),
      background-color var(--transition), transform var(--transition);
  }
  .feature-card:hover {
    box-shadow: var(--shadow-hover);
    transform: translateY(-2px);
  }
  /* ---- Producto: cada bloque agrupa un producto y sus tarjetas ---- */
  .product-block + .product-block {
    margin-top: var(--space-12);
  }
  .product-block__head {
    max-width: 720px;
    margin-bottom: var(--space-6);
  }
  .product-badge {
    display: inline-block;
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 1px;
    text-transform: uppercase;
    border-radius: 20px;
    padding: 5px 14px;
    margin-bottom: var(--space-3);
    border: 1px solid;
  }
  .product-badge--luisa {
    color: var(--violet);
    border-color: rgba(108, 99, 255, 0.4);
    background: rgba(108, 99, 255, 0.08);
  }
  .product-badge--admin {
    color: var(--teal-deep);
    border-color: rgba(0, 199, 180, 0.4);
    background: rgba(0, 199, 180, 0.08);
  }
  .product-block__tagline {
    color: var(--muted);
    font-size: 16px;
    line-height: var(--lh-body);
    margin: 0;
  }
  a.product-badge {
    text-decoration: none;
    transition: transform var(--transition), box-shadow var(--transition);
  }
  a.product-badge:hover {
    transform: translateY(-1px);
    box-shadow: var(--shadow-sm);
  }
  .product-block__link {
    display: inline-block;
    margin-top: var(--space-3);
    font-size: var(--text-small);
    font-weight: 700;
    text-decoration: none;
  }
  .product-block__link:hover {
    text-decoration: underline;
  }
  .product-block__link--luisa {
    color: var(--violet);
  }
  .product-block__link--admin {
    color: var(--teal-deep);
  }
  .feature-card__icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 44px;
    height: 44px;
    border-radius: 12px;
    margin-bottom: var(--space-4);
    transition: background-color var(--transition), color var(--transition);
  }
  .feature-card__title {
    font-size: var(--text-h3);
    margin: 0 0 var(--space-2);
  }
  .feature-card__desc {
    color: var(--muted);
    font-size: var(--text-small);
    line-height: var(--lh-body);
    margin: 0;
  }

  /* Producto: Empleados IA — violeta (exclusivo de Luisa IA en el DS) */
  .feature-card--luisa .feature-card__icon {
    background: rgba(108, 99, 255, 0.1);
    color: var(--violet);
  }
  .feature-card--luisa:hover {
    background: rgba(108, 99, 255, 0.07);
    border-color: rgba(108, 99, 255, 0.45);
  }
  .feature-card--luisa:hover .feature-card__icon {
    background: var(--grad-violet);
    color: #fff;
  }

  /* Producto: Plataforma Comercial — teal (acento primario del DS) */
  .feature-card--admin .feature-card__icon {
    background: rgba(0, 199, 180, 0.1);
    color: var(--teal-deep);
  }
  .feature-card--admin:hover {
    background: rgba(0, 199, 180, 0.07);
    border-color: rgba(0, 199, 180, 0.45);
  }
  .feature-card--admin:hover .feature-card__icon {
    background: var(--grad-cta);
    color: #fff;
  }

  /* ---- Integraciones: dos cintas contra-rotantes de sistemas ---- */
  .integrations {
    display: grid;
    gap: var(--space-4);
  }
  .integrations__marquee {
    overflow: hidden;
    /* desvanecer en los bordes */
    mask-image: linear-gradient(to right, transparent, black 12%, black 88%, transparent);
    -webkit-mask-image: linear-gradient(to right, transparent, black 12%, black 88%, transparent);
  }
  .integrations__track {
    display: flex;
    gap: var(--space-6);
    width: max-content;
    padding-block: var(--space-2); /* aire para la sombra de las pills */
    animation: integrations-scroll 40s linear infinite;
  }
  .integrations__track--reverse {
    animation-direction: reverse;
    animation-duration: 48s; /* velocidad distinta: se siente orgánico */
  }
  .integrations__group {
    display: flex;
    gap: var(--space-6);
  }
  .integrations__marquee:hover .integrations__track {
    animation-play-state: paused;
  }
  @keyframes integrations-scroll {
    /* -50% menos medio gap: el loop se repite sin salto */
    to { transform: translateX(calc(-50% - var(--space-3))); }
  }
  .integration-pill {
    display: inline-flex;
    align-items: center;
    white-space: nowrap;
    padding: 14px 28px;
    /* fondo blanco fijo: los wordmarks oscuros deben leerse también en dark */
    background: #ffffff;
    border: 1px solid var(--border);
    border-radius: 999px;
    box-shadow: var(--shadow-sm);
    transition: border-color var(--transition), transform var(--transition);
  }
  .integration-pill:hover {
    border-color: var(--teal);
    transform: translateY(-2px);
  }
  .integration-pill__logo {
    height: 30px;
    width: auto;
    max-width: 150px;
    object-fit: contain;
    display: block;
  }
  .integrations__note {
    text-align: center;
    font-size: var(--text-small);
    color: var(--muted);
    margin-top: var(--space-2);
  }
  .integrations__note a {
    color: var(--teal-deep);
    font-weight: 600;
    text-decoration: none;
  }
  .integrations__note a:hover {
    text-decoration: underline;
  }
  @media (prefers-reduced-motion: reduce) {
    .integrations__track {
      animation: none;
      width: auto;
    }
    .integrations__track,
    .integrations__group {
      flex-wrap: wrap;
      justify-content: center;
    }
    /* sin animación no hace falta la copia duplicada */
    .integrations__group[aria-hidden='true'] { display: none; }
    .integrations__marquee { mask-image: none; -webkit-mask-image: none; }
  }

  .pricing-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-6);
    align-items: stretch;
  }
  .setup-card {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: var(--space-8);
    background: var(--white);
    border: 1px solid var(--border);
    border-radius: var(--radius-card);
    box-shadow: var(--shadow-sm);
    padding: var(--space-8);
    margin-top: var(--space-6);
  }
  .setup-card__badge {
    display: inline-block;
    background: var(--grad-cta);
    color: #fff;
    font-size: 11px;
    font-weight: 600;
    letter-spacing: var(--ls-label);
    text-transform: uppercase;
    padding: 4px 14px;
    border-radius: 20px;
    margin-bottom: var(--space-3);
  }
  .setup-card__name {
    font-family: var(--font-display);
    font-size: var(--text-h3);
    font-weight: 700;
    color: var(--navy);
  }
  .setup-card__desc {
    font-size: var(--text-small);
    color: var(--muted);
    margin-top: var(--space-1);
    max-width: 560px;
  }
  .setup-card__features {
    list-style: none;
    padding: 0;
    margin: var(--space-4) 0 0;
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: var(--space-3) var(--space-6);
    font-size: 14.5px;
  }
  .setup-card__features li { display: flex; gap: var(--space-2); }
  .setup-card__check { color: var(--teal); font-weight: 700; }
  .setup-card__price-box {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    text-align: right;
    white-space: nowrap;
  }
  .setup-card__from { font-size: 13px; color: var(--muted); }
  .setup-card__price {
    font-family: var(--font-mono);
    font-size: 34px;
    font-weight: 700;
    color: var(--navy);
    letter-spacing: -1px;
  }
  .setup-card__period { font-size: 14px; color: var(--muted); }
  .pricing-terms {
    display: flex;
    justify-content: center;
    gap: var(--space-8);
    flex-wrap: wrap;
    margin-top: var(--space-12);
    font-size: 13px;
    color: var(--muted);
  }

  .faq-footer {
    text-align: center;
    max-width: 620px;
    margin: var(--space-12) auto 0;
    padding-top: var(--space-12);
    border-top: 1px solid var(--border);
  }
  .faq-footer p {
    font-size: var(--text-body);
    color: var(--muted);
    margin-bottom: var(--space-6);
  }

  @media (max-width: 1024px) {
    .features-grid { grid-template-columns: repeat(2, 1fr); }
    .feature-card { padding: var(--space-6) var(--space-4); }
    .pricing-grid { grid-template-columns: 1fr; max-width: 440px; margin-inline: auto; }
    .setup-card {
      flex-direction: column;
      align-items: flex-start;
      max-width: 440px;
      margin-inline: auto;
    }
    .setup-card__features { grid-template-columns: 1fr; }
    .setup-card__price-box { align-items: flex-start; text-align: left; }
  }
  @media (max-width: 640px) {
    .features-grid { grid-template-columns: 1fr; }
    .steps__nav { display: none; }
    .step-panel__desc { font-size: var(--text-body); }
  }
</style>
