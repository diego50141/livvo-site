---
import { Image } from 'astro:assets';
import { Icon } from 'astro-icon/components';
import Base from '../layouts/Base.astro';
import Navbar from '../components/Navbar.astro';
import Button from '../components/Button.astro';
import Section from '../components/Section.astro';
import FAQ from '../components/FAQ.astro';
import DealFlow from '../components/DealFlow.astro';
import Footer from '../components/Footer.astro';
import { WHATSAPP_URL } from '../config';
import fotoLuisa from '../assets/equipo/luisa.png';

// Orden de la home: problema → Luisa (IA) → Livvo Chat (plataforma) →
// plataforma completa (los dos juntos, con CTA a cada uno).
// El servicio administrado ("Livvo Administración") y el resto del equipo IA
// (Lucía, Laura, Lina, Lorena, Liliana) quedan fuera del home: el equipo vive
// en /agentes-ia, al que se llega desde el cierre de la sección de Luisa.
//
// Una familia de layout por sección (regla anti-plantilla): cifras, grilla de
// iconos, bento con capturas, lista a dos columnas, marquesina, CTA y
// acordeón. Ninguna se repite.

// El problema del sector: tres costos concretos de operar con herramientas
// sueltas y trabajo manual. Los números son el gancho; el título la tesis.
const problemStats = [
  {
    num: 4,
    suffix: ' h',
    title: 'Tu asesor pierde hasta 4 horas al día respondiendo manualmente.',
    desc: '¿Cuántas alcobas tiene? ¿Dónde queda? ¿Cuánto vale la cuota inicial? Tu equipo responde las mismas preguntas 50 veces al día en vez de enfocarse en cerrar negocios.',
  },
  {
    num: 3,
    suffix: '/10',
    title: 'Solo 3 de cada 10 citas agendadas se cumplen.',
    desc: 'Sin confirmación automática ni recordatorios, tus asesores pierden tiempo esperando clientes que no llegan.',
  },
  {
    num: 1,
    suffix: '%',
    title: 'Apenas el 1% de tus leads se convierte en negocios.',
    desc: 'Clientes que estaban listos para comprar se enfriaron porque nadie les hizo seguimiento a tiempo.',
  },
];

// Luisa: seis capacidades, en el orden real de una conversación
// (responder → calificar → recomendar → agendar → seguir → escalar).
const luisaSkills = [
  {
    icon: 'ph:clock',
    title: 'Responde en segundos, 24/7',
    desc: 'Nunca deja a un lead esperando. Madrugada, domingo o festivo: la primera respuesta llega en segundos y con el tono de tu marca.',
  },
  {
    icon: 'ph:funnel',
    title: 'Califica cada lead',
    desc: 'Pregunta por operación, presupuesto, zona y forma de pago, y arma el perfil completo antes de que un asesor invierta un minuto.',
  },
  {
    icon: 'ph:house-line',
    title: 'Recomienda propiedades',
    desc: 'Busca en tu portafolio y envía las opciones que encajan con lo que el cliente pidió, con fotos, precio y detalles.',
  },
  {
    icon: 'ph:calendar-check',
    title: 'Agenda la visita',
    desc: 'Cierra la cita en la misma conversación, la sincroniza con Google Calendar y después confirma y recuerda.',
  },
  {
    icon: 'ph:target',
    title: 'Rescata leads fríos',
    desc: 'Retoma a quien dejó de responder con seguimientos programados, en vez de dejar que el negocio se enfríe solo.',
  },
  {
    icon: 'ph:hand-arrow-up',
    title: 'Escala a tu asesor',
    desc: 'Cuando detecta intención real de compra o algo que no sabe, pasa la conversación a un humano con todo el contexto a la vista.',
  },
];

// Livvo Chat en bento sobre una grilla de 6 columnas. Los anchos alternan
// (4+2 / 2+4 / 3+3) para que no sea una grilla regular de tarjetas iguales:
// esa familia de layout ya la usa la sección de Luisa. Las dos celdas grandes
// van tintadas para que la sección tenga variación sin depender de imágenes.
const chatCells = [
  {
    key: 'bandeja',
    span: 4,
    tinted: true,
    icon: 'ph:tray',
    title: 'Bandeja compartida',
    desc: 'Todo el equipo atiende desde una sola bandeja: conversaciones asignables, notas internas, etiquetas e historial completo de cada cliente.',
  },
  {
    key: 'embudo',
    span: 2,
    icon: 'ph:kanban',
    title: 'Embudo inmobiliario',
    desc: 'De lead nuevo a cierre ganado, con las etapas del sector y los valores en pesos.',
  },
  {
    key: 'propiedades',
    span: 2,
    icon: 'ph:buildings',
    title: 'Gestor de propiedades',
    desc: 'Tu portafolio y tus clientes centralizados. Es la base de la que Luisa saca sus recomendaciones.',
  },
  {
    key: 'campanas',
    span: 4,
    tinted: true,
    icon: 'ph:megaphone-simple',
    title: 'Campañas y difusiones',
    desc: 'Envíos masivos por WhatsApp con plantillas aprobadas por Meta: nuevos proyectos, open house y seguimiento de cartera, con métricas de entrega.',
  },
  {
    key: 'panel',
    span: 3,
    icon: 'ph:chart-bar',
    title: 'Reportes de la operación',
    desc: 'Tiempos de respuesta, conversaciones atendidas, citas agendadas y negocios por asesor. Sabes qué está pasando sin preguntar.',
  },
  {
    key: 'automatizaciones',
    span: 3,
    icon: 'ph:lightning',
    title: 'Automatizaciones y API',
    desc: 'Reglas «si pasa X, haz Y», webhooks salientes y API pública para conectar Livvo con las herramientas que ya usas.',
  },
];

// Plataforma completa: el recap final con los dos productos y su CTA propio.
const products = [
  {
    key: 'chat',
    name: 'Livvo Chat',
    desc: 'La plataforma que centraliza todas las conversaciones de WhatsApp y otros canales en un solo lugar, con múltiples agentes.',
    href: '/plataforma-de-atencion-al-cliente',
    external: false,
  },
  {
    key: 'luisa',
    name: 'Luisa IA',
    desc: 'La inteligencia artificial que califica leads en tiempo real, agenda visitas automáticamente y mantiene tu inmobiliaria disponible 24 horas, 7 días a la semana.',
    href: '/agentes-ia',
    external: false,
  },
];

// Tecnología y seguridad: lista a dos columnas, sin tarjetas. Es una sección
// de confianza, no de producto: separadores finos en vez de cajas.
const techItems = [
  {
    icon: 'ph:whatsapp-logo',
    title: 'API oficial de WhatsApp Business',
    desc: 'Tu línea opera sobre la Meta Cloud API, no sobre soluciones no oficiales que ponen en riesgo tu número.',
  },
  {
    icon: 'ph:calendar-blank',
    title: 'Google Calendar integrado',
    desc: 'Integración nativa: coordinación de citas sin salir de Livvo.',
  },
  {
    icon: 'ph:cloud-check',
    title: 'Infraestructura en la nube',
    desc: 'Monitoreo 24/7. Tus agentes nunca dejan de trabajar.',
  },
  {
    icon: 'ph:database',
    title: 'Tus datos son tuyos',
    desc: 'Exportables vía API y alojados con seguridad de nivel empresarial.',
  },
  {
    icon: 'ph:clock-counter-clockwise',
    title: 'Historial completo de conversaciones',
    desc: 'Cada chat queda registrado y auditable. Nada se pierde, aunque un asesor se vaya.',
  },
  {
    icon: 'ph:shield-check',
    title: 'Roles y permisos por usuario',
    desc: 'Cada asesor ve lo suyo; tú tienes visibilidad de toda la operación.',
  },
];

// Integraciones — logos oficiales en public/integrations/.
// Dos cintas: CRMs (arriba) y portales inmobiliarios (abajo).
const integrationRows = [
  [
    { name: 'HubSpot', cat: 'CRM', src: '/integrations/hubspot.svg' },
    { name: 'GoHighLevel', cat: 'CRM', src: '/integrations/gohighlevel.svg' },
    { name: 'Wasi', cat: 'CRM inmobiliario', src: '/integrations/wasi.svg' },
    { name: 'Domus', cat: 'CRM inmobiliario', src: '/integrations/domus.png' },
  ],
  [
    { name: 'Fincaraíz', cat: 'Portal', src: '/integrations/fincaraiz.png' },
    { name: 'Metrocuadrado', cat: 'Portal', src: '/integrations/metrocuadrado.png' },
    { name: 'Ciencuadras', cat: 'Portal', src: '/integrations/ciencuadras.svg' },
    { name: 'Century 21', cat: 'Red inmobiliaria', src: '/integrations/century21.svg' },
  ],
];

const faqItems = [
  {
    q: '¿Qué hace exactamente Livvo?',
    a: 'Livvo son dos productos que trabajan juntos: Luisa, la agente de inteligencia artificial que atiende, califica y agenda por WhatsApp 24/7, y Livvo Chat, la plataforma donde tu equipo gestiona conversaciones, propiedades y negocios en un solo lugar. El objetivo es eliminar el trabajo operativo de tu equipo y aumentar tu tasa de cierre.',
  },
  {
    q: '¿Puedo usar la plataforma sin la IA?',
    a: 'Sí. Livvo Chat funciona por sí solo como CRM de WhatsApp: bandeja compartida, embudo, propiedades y campañas. Luisa se activa cuando quieras, y a partir de ahí trabaja sobre esa misma base. Por eso el resultado es mucho mejor con los dos que con uno solo.',
  },
  {
    q: '¿La IA reemplaza a mis asesores?',
    a: 'No. Imagínala como una primera línea que nunca duerme: Luisa responde, califica y agenda; tus asesores entran cuando hay una visita que atender o un negocio que cerrar, con todo el historial de la conversación a la vista, y pueden intervenir en cualquier momento si lo ven necesario.',
  },
  {
    q: '¿Qué pasa si la IA no sabe responder algo?',
    a: 'Escala la conversación a un asesor humano en el mismo chat, sin que el cliente tenga que repetir nada. Además tú defines el nivel de autonomía: puede responder sola o dejar el borrador listo para que alguien lo apruebe antes de enviarlo.',
  },
  {
    q: '¿Sirve solo para vender, o también para arriendos y postventa?',
    a: 'También. Además de Luisa hay agentes IA especializados en servicio, administración, cobranza, jurídico y escrituración, que atienden a inquilinos y propietarios: requisitos, mantenimiento, renovaciones y estado de un proceso. Puedes conocerlos en la página de Agentes IA.',
  },
  {
    q: '¿Funciona con los leads que llegan de los portales?',
    a: 'Sí. Los leads de Fincaraíz, Metrocuadrado, Ciencuadras o tu propia página entran a la misma bandeja de Livvo Chat y reciben respuesta en segundos, sin importar la hora.',
  },
  {
    q: '¿Puedo seguir usando el número de WhatsApp que ya tiene mi inmobiliaria?',
    a: 'Sí. Migramos tu línea actual a WhatsApp Business API para que tus clientes sigan escribiendo al mismo número, y todo el equipo atiende desde una bandeja compartida en vez de desde celulares sueltos.',
  },
  {
    q: '¿De quién son los contactos y las conversaciones?',
    a: 'De tu inmobiliaria. Contactos, historial y negocios viven en la plataforma, no en el celular del asesor: si alguien se va del equipo, la relación con el cliente se queda.',
  },
  {
    q: '¿Cuánto tiempo toma implementarlo?',
    a: 'Entre 1 y 3 días dependiendo de la personalización. Incluye entrenamiento del agente y conexión con WhatsApp Business API.',
  },
  {
    q: '¿De qué depende el precio?',
    a: 'Del volumen de conversaciones al mes, del número de propiedades que administres y de cuántos usuarios de tu equipo entran a la plataforma.',
  },
  {
    q: '¿Qué pasa si quiero cancelar?',
    a: 'No hay contrato de permanencia. Cancelas cuando quieras, sin penalidades, y te acompañamos durante la implementación para asegurarnos de que veas resultados desde el primer mes.',
  },
];

---

<Base
  title="Livvo: agenda más visitas sin contratar más asesores"
  description="Livvo son dos productos: Luisa, la agente de IA que atiende, califica y agenda citas por WhatsApp 24/7, y Livvo Chat, la plataforma donde tu equipo gestiona conversaciones, propiedades y negocios."
>
  <Navbar />

  <main>
  <!-- HERO: el paraguas, los dos productos en un solo lugar -->
  <div class="hero-scene">
    <div id="hero-particles" class="hero-scene__particles" aria-hidden="true"></div>
    <header class="hero">
      <div class="container-site hero__inner">
        <div class="hero__content" id="hero-content">
          <span class="hero__badge">
            Automatización para <span class="hero__badge-word" id="hero-badge-word">inmobiliarias</span>
          </span>
          <h1 class="hero__title">
            Agenda más visitas. <em class="grad-text">Sin contratar más asesores.</em>
          </h1>
          <p class="hero__sub">
            <strong>Luisa</strong> atiende, califica y agenda 24/7.
            <strong>Livvo Chat</strong> es la plataforma que unifica la atención al cliente.
          </p>
          <div class="hero__ctas">
            <Button href={WHATSAPP_URL} target="_blank" size="lg" id="hero-demo-cta">Prueba 15 días gratis →</Button>
          </div>
        </div>
        <div class="hero__chat">
          <DealFlow />
        </div>
      </div>
    </header>
  </div>

  <!-- EL PROBLEMA: cifras. Familia de layout propia (número protagonista) -->
  <Section
    id="problema"
    eyebrow="El problema"
    title="Tu operación comercial está fragmentada."
    alt
  >
    <div class="problem-grid fade-up">
      {problemStats.map(({ num, suffix, title, desc }) => (
        <article class="problem-card">
          <span class="problem-card__value">{num}{suffix}</span>
          <h3 class="problem-card__title">{title}</h3>
          <p class="problem-card__desc">{desc}</p>
        </article>
      ))}
    </div>
  </Section>

  <!-- LUISA: grilla de capacidades con icono. Única sección con esta familia -->
  <Section
    id="luisa"
    eyebrow="Inteligencia artificial"
    title="Conoce a Luisa, la agente que atiende por ti."
  >
    <div class="luisa-strip fade-up">
      <Image
        class="luisa-strip__photo"
        src={fotoLuisa}
        alt="Luisa, agente de IA de Livvo"
        width={220}
        densities={[1, 2]}
      />
      <div>
        <h3 class="luisa-strip__name">Luisa</h3>
        <span class="luisa-strip__rol">Agente comercial y de atención al cliente</span>
        <p class="luisa-strip__desc">
          Atiende por WhatsApp y por la web. Habla como tu equipo, conoce tu portafolio
          y no deja una sola consulta sin responder.
        </p>
      </div>
    </div>

    <div class="skills-grid fade-up">
      {luisaSkills.map(({ icon, title, desc }) => (
        <article class="skill-card">
          <span class="skill-card__icon" aria-hidden="true">
            <Icon name={icon} width={22} height={22} />
          </span>
          <h3 class="skill-card__title">{title}</h3>
          <p class="skill-card__desc">{desc}</p>
        </article>
      ))}
    </div>

    <div class="product-close fade-up">
      <div class="product-close__text">
        <h3 class="product-close__title">Luisa, tu nueva asistente virtual con IA.</h3>
        <p class="product-close__desc">
          Califica leads, agenda visitas y aumenta el número de citas de tu inmobiliaria
          con atención 24 horas, 7 días a la semana.
        </p>
      </div>
      <Button href="/agentes-ia" variant="outline" size="lg">Más información →</Button>
    </div>
  </Section>

  <!-- LIVVO CHAT: bento. Tres celdas con captura real, tres de texto -->
  <Section
    id="livvo-chat"
    eyebrow="Plataforma de atención al cliente"
    title="Livvo Chat: todo tu WhatsApp, ordenado."
    alt
  >
    <div class="bento fade-up">
      {chatCells.map(({ key, span, tinted, icon, title, desc }) => (
        <article
          class:list={['bento__cell', `bento__cell--${span}`, { 'bento__cell--tinted': tinted }]}
        >
          <span class="bento__icon" aria-hidden="true">
            <Icon name={icon} width={22} height={22} />
          </span>
          <h3 class="bento__title">{title}</h3>
          <p class="bento__desc">{desc}</p>
        </article>
      ))}
    </div>

    <div class="product-close fade-up">
      <div class="product-close__text">
        <h3 class="product-close__title">Unifica la atención al cliente de tu inmobiliaria.</h3>
        <p class="product-close__desc">
          Centraliza todas las conversaciones de WhatsApp en un solo lugar. Gestiona equipos,
          transfiere solicitudes y haz seguimiento de los resultados con una plataforma
          diseñada para el sector inmobiliario.
        </p>
      </div>
      <Button href="/plataforma-de-atencion-al-cliente" variant="outline" size="lg">
        Más información →
      </Button>
    </div>
  </Section>

  <!-- PLATAFORMA COMPLETA: los dos productos, cada uno con su CTA -->
  <Section
    id="plataforma-completa"
    eyebrow="Plataforma completa"
    title="Todo lo que tu inmobiliaria necesita para vender y atender."
  >
    <div class="product-grid fade-up">
      {products.map(({ key, name, desc, href, external }) => (
        <article class:list={['product-card', `product-card--${key}`]}>
          <h3 class="product-card__name">{name}</h3>
          <p class="product-card__desc">{desc}</p>
          <div class="product-card__cta">
            <Button href={href} target={external ? '_blank' : undefined} variant="outline">
              Más información →
            </Button>
          </div>
        </article>
      ))}
    </div>
  </Section>

  <!-- INTEGRACIONES: marquesina. Única del sitio en esta página -->
  <Section
    id="integraciones"
    eyebrow="Integraciones"
    title="Livvo se conecta con los sistemas que ya usas."
    alt
  >
    <div class="integrations fade-up">
      {integrationRows.map((row, i) => (
        <div class="integrations__marquee">
          <div class:list={['integrations__track', { 'integrations__track--reverse': i % 2 === 1 }]}>
            {[0, 1].map((copy) => (
              <div class="integrations__group" aria-hidden={copy === 1 ? 'true' : undefined}>
                {row.map(({ name, cat, src }) => (
                  <span class="integration-pill" title={`${name} · ${cat}`}>
                    <img class="integration-pill__logo" src={src} alt={name} loading="lazy" />
                  </span>
                ))}
              </div>
            ))}
          </div>
        </div>
      ))}
      <p class="integrations__note">
        ¿Usas otro sistema? <a href={WHATSAPP_URL} target="_blank" rel="noopener">Cuéntanos</a>.
      </p>
    </div>
  </Section>

  <!-- TECNOLOGÍA: lista a dos columnas, sin tarjetas (sección de confianza) -->
  <Section
    id="tecnologia"
    eyebrow="Tecnología y seguridad"
    title="Tecnología en la que puedes confiar."
  >
    <ul class="trust-list fade-up">
      {techItems.map(({ icon, title, desc }) => (
        <li class="trust-item">
          <span class="trust-item__icon" aria-hidden="true">
            <Icon name={icon} width={20} height={20} />
          </span>
          <div>
            <h3 class="trust-item__title">{title}</h3>
            <p class="trust-item__desc">{desc}</p>
          </div>
        </li>
      ))}
    </ul>
  </Section>

  <!-- PLANES: cierre hacia /planes -->
  <Section
    id="planes"
    eyebrow="Planes"
    title="Los dos productos, un solo plan."
    alt
  >
    <div class="plan-cta fade-up">
      <ul class="plan-cta__terms">
        <li>Prueba gratuita de 15 días</li>
        <li>Sin tarjeta de crédito</li>
        <li>Sin contrato de permanencia</li>
      </ul>
      <div class="plan-cta__buttons">
        <Button href="/planes" size="lg">Ver planes y precios →</Button>
      </div>
    </div>
  </Section>

  <!-- FAQ: acordeón -->
  <Section id="faq" eyebrow="Preguntas frecuentes" title="Lo que nos preguntan antes de empezar.">
    <div class="fade-up">
      <FAQ items={faqItems} />
      <div class="faq-footer fade-up">
        <p>Contáctanos si tienes alguna pregunta o problema.</p>
        <Button href={WHATSAPP_URL} target="_blank" variant="primary">Prueba 15 días gratis →</Button>
      </div>
    </div>
  </Section>
  </main>

  <Footer />
</Base>

<script>
  // Partículas teal del DS: pocas, pequeñas, deriva lenta y parpadeo sutil.
  // Sin top-level await: si algo falla o se cuelga, no bloquea el módulo.
  import { tsParticles } from '@tsparticles/engine';
  import { loadSlim } from '@tsparticles/slim';

  const initParticles = async () => {
    await loadSlim(tsParticles);
    await tsParticles.load({
      id: 'hero-particles',
      options: {
        fullScreen: { enable: false },
        background: { color: 'transparent' },
        fpsLimit: 60,
        detectRetina: true,
        particles: {
          number: { value: 18, density: { enable: false } },
          color: { value: '#00C7B4' },
          shape: { type: 'circle' },
          opacity: {
            value: { min: 0.3, max: 0.8 },
            animation: { enable: true, speed: 0.5, sync: false },
          },
          size: { value: { min: 4, max: 7 } },
          shadow: { enable: true, color: '#00C7B4', blur: 18, offset: { x: 0, y: 0 } },
          move: {
            enable: true,
            speed: 0.35,
            direction: 'none',
            random: true,
            straight: false,
            outModes: { default: 'out' },
          },
        },
        interactivity: {
          events: { onHover: { enable: false }, onClick: { enable: false }, resize: { enable: true } },
        },
      },
    });
  };

  if (!matchMedia('(prefers-reduced-motion: reduce)').matches) {
    initParticles().catch((e) => console.error('[particles] error:', e));
  }
</script>

<script>
  // Badge del hero: la vertical rota entre los tres segmentos con un
  // crossfade breve; con reduced-motion el texto cambia sin animación.
  const badgeWord = document.getElementById('hero-badge-word');
  const badgeReduced = matchMedia('(prefers-reduced-motion: reduce)').matches;
  const verticals = ['inmobiliarias', 'constructoras', 'desarrolladoras inmobiliarias'];
  let verticalIdx = 0;

  if (badgeWord) {
    setInterval(() => {
      verticalIdx = (verticalIdx + 1) % verticals.length;
      if (badgeReduced) {
        badgeWord.textContent = verticals[verticalIdx];
        return;
      }
      badgeWord.classList.add('is-out');
      setTimeout(() => {
        badgeWord.textContent = verticals[verticalIdx];
        badgeWord.classList.remove('is-out');
      }, 250);
    }, 3000);
  }
</script>

<style>
  /* La escena contiene hero + chat y aporta el fondo */
  .hero-scene {
    position: relative;
    overflow: hidden;
    background: var(--white);
    transition: background-color var(--transition);
  }

  /* Partículas teal (patrón del DS: dispersas, energía ambiental) */
  .hero-scene__particles {
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
  }

  .hero {
    min-height: calc(82vh - 68px);
    display: flex;
    align-items: center;
    background: transparent;
    position: relative;
    z-index: 2;
    padding: 10vh 0;
  }
  /* Hero a dos columnas: mensaje a la izquierda, conversación viva a la derecha */
  .hero__inner {
    width: 100%;
    display: grid;
    grid-template-columns: 1fr minmax(420px, 560px);
    align-items: center;
    gap: var(--space-12);
  }
  .hero__content {
    animation: hero-in 0.7s ease both;
    will-change: transform, opacity;
  }
  @keyframes hero-in {
    from { opacity: 0; transform: translateY(24px); }
    to { opacity: 1; transform: none; }
  }
  /* Badge con vertical rotatoria: inmobiliarias / constructoras / desarrolladoras */
  .hero__badge {
    display: inline-block;
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 1px;
    text-transform: uppercase;
    border-radius: 20px;
    padding: 6px 16px;
    margin-bottom: var(--space-6);
    border: 1px solid rgba(0, 199, 180, 0.4);
    color: var(--teal-deep);
    background: rgba(0, 199, 180, 0.08);
  }
  .hero__badge-word {
    display: inline-block;
    transition: opacity 0.25s ease, transform 0.25s ease;
  }
  .hero__badge-word.is-out {
    opacity: 0;
    transform: translateY(6px);
  }

  .hero__title {
    font-size: clamp(36px, 4vw, 56px);
    letter-spacing: -1.5px;
    line-height: 1.1;
    margin: 0 0 var(--space-6);
  }
  .hero__title em { font-style: normal; }
  /* subtítulo corto a propósito: el hero entero debe caber en el viewport */
  .hero__sub {
    font-size: 18px;
    line-height: var(--lh-relaxed);
    color: var(--muted);
    max-width: 460px;
    margin: 0 0 var(--space-8);
  }
  .hero__sub strong {
    color: var(--text);
    font-weight: 700;
  }
  .hero__ctas { display: flex; gap: var(--space-4); flex-wrap: wrap; }
  .hero__chat {
    display: block;
    animation: hero-in 0.7s 0.15s ease both;
  }

  @media (max-width: 900px) {
    .hero__inner {
      grid-template-columns: 1fr;
      gap: var(--space-8);
      justify-items: center;
      text-align: center;
    }
    .hero__sub { margin-inline: auto; }
    .hero__ctas { justify-content: center; }
    .hero__chat { justify-content: center; width: 100%; }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero__content { animation: none; }
  }

  /* ---- El problema: la cifra es el protagonista ---- */
  .problem-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-6);
    align-items: stretch;
  }
  .problem-card {
    background: var(--white);
    border: 1px solid var(--border);
    border-radius: var(--radius-card);
    box-shadow: var(--shadow-sm);
    padding: var(--space-8) var(--space-6);
    transition: box-shadow var(--transition), transform var(--transition);
  }
  .problem-card:hover {
    box-shadow: var(--shadow-hover);
    transform: translateY(-2px);
  }
  .problem-card__value {
    display: block;
    font-family: var(--font-mono);
    font-size: 44px;
    font-weight: 700;
    color: var(--red);
    letter-spacing: -1px;
    margin-bottom: var(--space-4);
  }
  .problem-card__title {
    font-size: var(--text-h3);
    margin: 0 0 var(--space-2);
  }
  .problem-card__desc {
    color: var(--muted);
    font-size: var(--text-small);
    line-height: var(--lh-body);
    margin: 0;
  }
  @media (max-width: 900px) {
    .problem-grid { grid-template-columns: 1fr; }
  }

  /* ---- Luisa: ficha de identidad + grilla de capacidades ---- */
  .luisa-strip {
    display: flex;
    align-items: center;
    gap: var(--space-8);
    max-width: 720px;
    margin: 0 auto var(--space-12);
    padding: var(--space-8);
    background: var(--white);
    border: 1px solid var(--border);
    border-radius: var(--radius-card);
    box-shadow: var(--shadow-sm);
  }
  .luisa-strip__photo {
    width: 110px;
    height: 110px;
    border-radius: 50%;
    object-fit: cover;
    flex-shrink: 0;
    border: 3px solid rgba(108, 99, 255, 0.4);
  }
  .luisa-strip__name {
    font-size: var(--text-h2);
    margin: 0;
  }
  .luisa-strip__rol {
    display: inline-block;
    margin-top: var(--space-1);
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 1px;
    text-transform: uppercase;
    border-radius: 20px;
    padding: 4px 12px;
    color: var(--violet);
    border: 1px solid rgba(108, 99, 255, 0.4);
    background: rgba(108, 99, 255, 0.08);
  }
  .luisa-strip__desc {
    color: var(--muted);
    font-size: var(--text-small);
    line-height: var(--lh-body);
    margin: var(--space-4) 0 0;
  }
  @media (max-width: 640px) {
    .luisa-strip {
      flex-direction: column;
      text-align: center;
      padding: var(--space-6);
      gap: var(--space-6);
    }
  }

  .skills-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-6);
  }
  .skill-card {
    background: var(--white);
    border: 1px solid var(--border);
    border-radius: var(--radius-card);
    box-shadow: var(--shadow-sm);
    padding: var(--space-6);
    transition: box-shadow var(--transition), border-color var(--transition),
      background-color var(--transition), transform var(--transition);
  }
  .skill-card:hover {
    box-shadow: var(--shadow-hover);
    background: rgba(108, 99, 255, 0.07);
    border-color: rgba(108, 99, 255, 0.45);
    transform: translateY(-2px);
  }
  .skill-card__icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 44px;
    height: 44px;
    border-radius: 12px;
    margin-bottom: var(--space-4);
    background: rgba(108, 99, 255, 0.1);
    color: var(--violet);
    transition: background-color var(--transition), color var(--transition);
  }
  .skill-card:hover .skill-card__icon {
    background: var(--violet);
    color: #fff;
  }
  .skill-card__title {
    font-size: var(--text-h3);
    margin: 0 0 var(--space-2);
  }
  .skill-card__desc {
    color: var(--muted);
    font-size: var(--text-small);
    line-height: var(--lh-body);
    margin: 0;
  }
  @media (max-width: 1024px) {
    .skills-grid { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 640px) {
    .skills-grid { grid-template-columns: 1fr; }
  }

  /* ---- Livvo Chat en bento: 6 celdas sobre 6 columnas (4+2 / 2+4 / 3+3) ---- */
  .bento {
    display: grid;
    grid-template-columns: repeat(6, 1fr);
    gap: var(--space-4);
  }
  .bento__cell {
    background: var(--white);
    border: 1px solid var(--border);
    border-radius: var(--radius-card);
    box-shadow: var(--shadow-sm);
    padding: var(--space-8) var(--space-6);
    transition: box-shadow var(--transition), border-color var(--transition),
      background-color var(--transition), transform var(--transition);
  }
  .bento__cell:hover {
    box-shadow: var(--shadow-hover);
    border-color: rgba(0, 199, 180, 0.45);
    transform: translateY(-2px);
  }
  /* los anchos alternan para romper la rejilla regular */
  .bento__cell--4 { grid-column: span 4; }
  .bento__cell--3 { grid-column: span 3; }
  .bento__cell--2 { grid-column: span 2; }

  /* las dos celdas grandes van tintadas: variación sin depender de imágenes */
  .bento__cell--tinted {
    background: rgba(0, 199, 180, 0.06);
    border-color: rgba(0, 199, 180, 0.22);
  }

  .bento__icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 44px;
    height: 44px;
    border-radius: 12px;
    margin-bottom: var(--space-4);
    background: rgba(0, 199, 180, 0.1);
    color: var(--teal-deep);
    transition: background-color var(--transition), color var(--transition);
  }
  .bento__cell--tinted .bento__icon { background: rgba(0, 199, 180, 0.16); }
  .bento__cell:hover .bento__icon {
    background: var(--grad-cta);
    color: #fff;
  }
  .bento__title {
    font-size: var(--text-h3);
    margin: 0 0 var(--space-2);
  }
  .bento__desc {
    color: var(--muted);
    font-size: var(--text-small);
    line-height: var(--lh-body);
    margin: 0;
  }
  /* tablet: 2 columnas, la grande ocupa el ancho completo */
  @media (max-width: 900px) {
    .bento { grid-template-columns: repeat(2, 1fr); }
    .bento__cell--4 { grid-column: span 2; }
    .bento__cell--3,
    .bento__cell--2 { grid-column: span 1; }
  }
  @media (max-width: 560px) {
    .bento { grid-template-columns: 1fr; }
    .bento__cell--4,
    .bento__cell--3,
    .bento__cell--2 { grid-column: span 1; }
  }

  /* ---- Banda de cierre de cada producto ---- */
  .product-close {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-8);
    max-width: 960px;
    margin: var(--space-12) auto 0;
    padding: var(--space-8);
    background: var(--white);
    border: 1px solid var(--border);
    border-radius: var(--radius-card);
    box-shadow: var(--shadow-sm);
  }
  .product-close__text { max-width: 560px; }
  .product-close__title {
    font-size: var(--text-h2);
    margin: 0 0 var(--space-2);
  }
  .product-close__desc {
    color: var(--muted);
    font-size: var(--text-small);
    line-height: var(--lh-body);
    margin: 0;
  }
  @media (max-width: 900px) {
    .product-close {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--space-6);
      padding: var(--space-6);
    }
  }

  /* ---- Plataforma completa: los dos productos con su CTA ---- */
  .product-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-6);
    align-items: stretch;
    max-width: 960px;
    margin-inline: auto;
  }
  .product-card {
    display: flex;
    flex-direction: column;
    background: var(--white);
    border: 1px solid var(--border);
    border-radius: var(--radius-card);
    box-shadow: var(--shadow-sm);
    padding: var(--space-8);
    transition: box-shadow var(--transition), border-color var(--transition),
      transform var(--transition);
  }
  .product-card:hover {
    box-shadow: var(--shadow-hover);
    transform: translateY(-2px);
  }
  .product-card--luisa:hover { border-color: rgba(108, 99, 255, 0.45); }
  .product-card--chat:hover { border-color: rgba(0, 199, 180, 0.45); }
  .product-card__name {
    font-size: var(--text-h2);
    margin: 0 0 var(--space-3);
  }
  .product-card--luisa .product-card__name { color: var(--violet); }
  .product-card--chat .product-card__name { color: var(--teal-deep); }
  .product-card__desc {
    color: var(--muted);
    font-size: var(--text-small);
    line-height: var(--lh-body);
    margin: 0 0 var(--space-8);
  }
  .product-card__cta { margin-top: auto; }
  @media (max-width: 900px) {
    .product-grid { grid-template-columns: 1fr; }
  }

  /* ---- Integraciones: dos cintas contra-rotantes ---- */
  .integrations {
    display: grid;
    gap: var(--space-4);
  }
  .integrations__marquee {
    overflow: hidden;
    mask-image: linear-gradient(to right, transparent, black 12%, black 88%, transparent);
    -webkit-mask-image: linear-gradient(to right, transparent, black 12%, black 88%, transparent);
  }
  .integrations__track {
    display: flex;
    gap: var(--space-6);
    width: max-content;
    padding-block: var(--space-2);
    animation: integrations-scroll 40s linear infinite;
  }
  .integrations__track--reverse {
    animation-direction: reverse;
    animation-duration: 48s;
  }
  .integrations__group {
    display: flex;
    gap: var(--space-6);
  }
  .integrations__marquee:hover .integrations__track {
    animation-play-state: paused;
  }
  @keyframes integrations-scroll {
    to { transform: translateX(calc(-50% - var(--space-3))); }
  }
  .integration-pill {
    display: inline-flex;
    align-items: center;
    white-space: nowrap;
    padding: 14px 28px;
    /* fondo blanco fijo: los wordmarks oscuros deben leerse también en dark */
    background: #ffffff;
    border: 1px solid var(--border);
    border-radius: 999px;
    box-shadow: var(--shadow-sm);
    transition: border-color var(--transition), transform var(--transition);
  }
  .integration-pill:hover {
    border-color: var(--teal);
    transform: translateY(-2px);
  }
  .integration-pill__logo {
    height: 30px;
    width: auto;
    max-width: 150px;
    object-fit: contain;
    display: block;
  }
  .integrations__note {
    text-align: center;
    font-size: var(--text-small);
    color: var(--muted);
    margin-top: var(--space-2);
  }
  .integrations__note a {
    color: var(--teal-deep);
    font-weight: 600;
    text-decoration: none;
  }
  .integrations__note a:hover {
    text-decoration: underline;
  }
  @media (prefers-reduced-motion: reduce) {
    .integrations__track {
      animation: none;
      width: auto;
    }
    .integrations__track,
    .integrations__group {
      flex-wrap: wrap;
      justify-content: center;
    }
    .integrations__group[aria-hidden='true'] { display: none; }
    .integrations__marquee { mask-image: none; -webkit-mask-image: none; }
  }

  /* ---- Tecnología: lista a dos columnas, sin tarjetas ---- */
  .trust-list {
    list-style: none;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0 var(--space-12);
    max-width: 980px;
    margin-inline: auto;
    padding: 0;
  }
  .trust-item {
    display: flex;
    align-items: flex-start;
    gap: var(--space-4);
    padding: var(--space-6) 0;
    border-bottom: 1px solid var(--border);
  }
  /* sin línea bajo la última fila de cada columna */
  .trust-item:nth-last-child(-n + 2) { border-bottom: none; }
  .trust-item__icon {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    width: 38px;
    height: 38px;
    border-radius: 10px;
    background: rgba(0, 199, 180, 0.1);
    color: var(--teal-deep);
  }
  .trust-item__title {
    font-size: var(--text-h3);
    margin: 0 0 var(--space-1);
  }
  .trust-item__desc {
    color: var(--muted);
    font-size: var(--text-small);
    line-height: var(--lh-body);
    margin: 0;
  }
  @media (max-width: 768px) {
    .trust-list { grid-template-columns: 1fr; gap: 0; }
    .trust-item:nth-last-child(-n + 2) { border-bottom: 1px solid var(--border); }
    .trust-item:last-child { border-bottom: none; }
  }

  /* ---- Planes: cierre hacia /planes ---- */
  .plan-cta {
    text-align: center;
    max-width: 720px;
    margin-inline: auto;
  }
  .plan-cta__terms {
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--space-3) var(--space-8);
    margin: 0 0 var(--space-8);
    padding: 0;
  }
  .plan-cta__terms li {
    position: relative;
    padding-left: 24px;
    font-size: var(--text-small);
    color: var(--muted);
  }
  .plan-cta__terms li::before {
    content: '✓';
    position: absolute;
    left: 0;
    top: 0;
    font-weight: 700;
    color: var(--teal-deep);
  }
  .plan-cta__buttons {
    display: flex;
    justify-content: center;
    gap: var(--space-4);
    flex-wrap: wrap;
  }

  .faq-footer {
    text-align: center;
    max-width: 620px;
    margin: var(--space-12) auto 0;
    padding-top: var(--space-12);
    border-top: 1px solid var(--border);
  }
  .faq-footer p {
    font-size: var(--text-body);
    color: var(--muted);
    margin-bottom: var(--space-6);
  }
</style>
